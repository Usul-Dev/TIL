coverage:
  status:
    project:
      default:
        target: 65%
        threshold: 0%
        flags:
          - redis
    patch:
      default:
        target: 65%
        threshold: 0%
        flags:
          - redis
